[project]
name = "u-teach-me"
version = "0.1.0"
description = "A GUI Agent based on IDM & OpenAI VPT."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pillow>=12.2.0",
    "pynput>=1.8.1",
]
